# 設計意図(よく読んで)

まず、現在のLLMには連続性がありません。そこで、LLMの外部にステートマシンを持たせ、LLMはそのステートマシンのトリガーを受け取り実行されるようにすることで連続性をシミュレーションします。
たとえば、Discord Botを考えます。通常であればメンションされたらトリガー/返信、か30分おきにMCPでチェックして必要に応じて返信などになるでしょう。単調で、とても人間とは言えません。しかしこのアプローチは違います。まず、つねにDiscordのメッセージイベントを監視します。いちいちLLMに渡していたらとんでもないコストがかかるので、ステートマシンに渡します。ステートマシンにはTransiotionsという計算式が定義されており、メッセージを受け取るたびに内部パラメータを調整します。たとえばboredomを下げて、want_to_speakを上げる、などです。するとここで一つのトランジションエンジンが生まれます。次に必要なのは、この値を監視して行動に変換するステートマシンです。たとえば、boredomが上昇してきて、web_attentionが上がってきたら、Web検索で暇つぶしをする、という行動に変換する必要があります。ここで一つ問題があります。Transitionの変化式、また行動変換の条件は、人間がハードコードするのでしょうか?答えはNOです。それはとてもつまらないので、内省というプロセスを挟みます。この内省は、アクションのトリガーではないので、定期実行でも問題ありません。30分おきに自分の行動を振り返り、条件式をLLM自身が目標とするキャラクター像に合わせて改善させていくのです。こうすることで、もはや開発者にも予測できない、真の自立性を得られると考えています。
